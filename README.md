# Enclave

AI-ассистент для малого бизнеса, который целиком работает внутри контура: документы,
эмбеддинги, модель и каждый запрос остаются на машине пользователя или в его
локальной сети. Отвечает по документам организации (RAG) голосом, подходящим
департаменту (LoRA-адаптеры на департамент), и гарантирует, что сотрудник
одного департамента **физически не может** достать чужие документы — изоляция
держится в базе данных, а не в коде приложения.

Проектная документация — [Design Document](Enclave%20—%20Design%20Document.md)
(требования, критерии приёмки, диаграммы, матрица доступа, риски) и
[docs/adr](docs/adr/README.md), двадцать три принятые Architecture Decision
Records. Код следует этим записям, а не наоборот:
инварианты, которые нельзя нарушать, перечислены в [CLAUDE.md](CLAUDE.md), и у
каждого есть ссылка на ADR с обоснованием.

## Быстрый старт

Нужны Rust (стабильный; CI собирает на 1.96.0), Node 20+ и pnpm. Для своей
базы приложения — ещё PostgreSQL 18 от EDB (из него берутся файлы сервера,
сама установка не трогается) и Visual Studio Build Tools с C++ (сборка
pgvector).

```bash
cd enclave && pnpm install
powershell -File .\scripts\fetch-postgres.ps1   # один раз: PostgreSQL 18 + pgvector, 61 МБ
pnpm tauri dev
```

**Своя база (по умолчанию, ADR-0014).** Если ни одна переменная
`*_DATABASE_URL` не задана, приложение само запускает PostgreSQL из
`src-tauri/binaries/pg`. Docker не нужен.

- **Первый запуск** занимает 5–15 с. Приложение создаёт каталог данных
  `%APPDATA%\com.softwarean.enclave\pgdata` и генерирует пароли трёх ролей,
  которые хранит зашифрованными DPAPI в `db-secrets.bin`.
- **Сервер** доступен только с `127.0.0.1`, порт выбирается свободный при
  каждом запуске.
- **При выходе** приложение останавливает сервер штатно. Если процесс
  убили, следующий старт восстанавливается по журналу.
- **Файлы сервера.** `fetch-postgres.ps1` берёт их из
  `C:\Program Files\PostgreSQL\18` или из распакованного архива бинарников
  EDB (`-PgSource`). pgvector собирает из исходников.

**Существующий сервер** (разработка, развёртывание в сети) — все три
переменные сразу:

```bash
docker run -d --name enclave-db -p 5433:5432 \
  -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=enclave \
  pgvector/pgvector:pg16

export ADMIN_DATABASE_URL="postgres://postgres:postgres@localhost:5433/enclave"
export APP_DATABASE_URL="postgres://enclave_app:change_me_in_production@localhost:5433/enclave"
export INGEST_DATABASE_URL="postgres://ingest_worker:change_me_in_production@localhost:5433/enclave"
```

Если задана только часть переменных, приложение не запустится: это ошибка,
а не отдельный режим.

Миграции (`enclave/migrations/001-016`) применяются автоматически при каждом
старте через `sqlx::migrate!` под ролью администратора. Они же создают роли
`enclave_app` (запросы под RLS) и `ingest_worker` (BYPASSRLS, только воркер
загрузки). У своей базы ядро после миграций меняет пароли этих ролей на
сгенерированные: пароль `change_me_in_production` из миграции там не пускает.
Первый созданный профиль становится администратором. Новый профиль попадает
только в общий департамент General; в остальные департаменты пользователей
добавляет администратор (ADR-0016).

Три строки подключения — это три роли, а не три способа записать одну и ту
же строку. Подменять `APP_DATABASE_URL` на суперпользователя нельзя: RLS при
этом молча выключается (ADR-0008).

**Движок и модели приложение скачивает само при первом запуске (ADR-0024,
ADR-0025).** Пока их нет, поверх любого экрана видна панель с кнопкой
загрузки.

- **Движок** — llama.cpp b11124 под видеокарту машины. На NVIDIA с
  драйвером CUDA ≥ 12.4 это CUDA (0,65 ГБ), при любом драйвере Vulkan —
  Vulkan (32 МБ), иначе CPU (19 МБ). Выбор можно задать вручную:
  `ENCLAVE_ENGINE=cuda|vulkan|cpu`. `scripts/fetch-sidecar.ps1` нужен только
  разработке без интернета.
- **Сетевые сбои** загрузчик повторяет сам, каждый раз продолжая с места
  обрыва.

- **Что скачивается:** Qwen3-4B (чат, 2,50 ГБ) и nomic-embed-text v1.5
  (эмбеддинги, 768, 0,27 ГБ), обе под Apache 2.0, из официальных
  репозиториев на Hugging Face. Файлы проверяются по закреплённому SHA-256.
- **Прерванная загрузка** продолжается с места обрыва.
- **Офлайн-машина:** в той же панели можно указать уже имеющийся `.gguf`.
- **Куда кладутся:** `%APPDATA%\com.softwarean.enclave\models\` (или
  `ENCLAVE_MODELS_DIR`). Установленными считаются только файлы, записанные в
  `manifest.json`.
- **Почему не Qwen2.5-3B, на которой проект начинался:** её лицензия
  `qwen-research` ограничивает коммерческое использование.

Ollama как среда исполнения не подходит: выбор LoRA-адаптера на каждый запрос
есть только у `llama-server` (ADR-0003, ADR-0004).

Без этого приложение поднимается и работает, но генерация и приём документов
отваливаются с внятной ошибкой «sidecar unavailable» — это заложенный путь
деградации, а не поломка. Путь к весам ядро превращает в абсолютный и проверяет
до запуска sidecar'а: отсутствующий файл даёт ошибку с полным путём, а не
минутное ожидание `/health`.

## Структура

```
docs/adr/              двадцать три ADR — источник истины по «почему»
docs/HANDOFF.md        состояние работ: что сделано, что открыто, следующий шаг
CLAUDE.md              бриф для агента: что строить и какие инварианты нельзя нарушать
db/schema.sql          исходный референсный проект схемы; не применяется нигде

enclave/                                  само приложение
  migrations/*.sql       единственный источник истины по схеме: pgvector, HNSW + GIN,
                         RLS-политики, роли app_user / enclave_app / ingest_worker
  src-tauri/src/
    lib.rs               старт: три пула, миграции, sidecar, фоновый воркер
    db/rls.rs            set_config('app.current_user_id', …, true) — транзакционно
    session.rs           кто вошёл: выставляет только cmd_login, команды берут личность отсюда
    audit.rs             журнал аудита — запись в той же транзакции, что и действие
    retrieval/mod.rs     гибридный поиск: плотная ветка ANN + лексическая FTS
    retrieval/rrf.rs     слияние рангов по RRF, k = 60 (ADR-0006)
    ingest/mod.rs        нарезка на фрагменты, эмбеддинги, запись chunks + векторов
    ingest/extract.rs    текст из PDF / DOCX / таблиц / TXT / MD (ADR-0019, ADR-0020)
    ingest/jobs.rs       воркер: claim через FOR UPDATE SKIP LOCKED, реапер сирот
    tables/              таблицы как таблицы: типы столбцов, план модели, агрегат (ADR-0022)
    llm/mod.rs           два sidecar'а llama-server: чат (8080) и эмбеддинги (8081)
    llm/adapters.rs      LoRA пользователя → id, выданные сервером (инвариант №5)
    commands/            тонкие обёртки Tauri: auth, documents, query, admin
  src/pages/             Login / Chat (стриминг + цитаты) / Documents / Admin
  src-tauri/tests/       rls_validation — доказательство изоляции департаментов;
                         retrieval_ranking, table_aggregation — поиск и расчёты под RLS
  scripts/               fetch-sidecar.ps1 — разовая загрузка бинарника llama.cpp
```

## Изоляция департаментов

Ядро проекта. Изоляцию обеспечивает PostgreSQL, а не приложение (ADR-0008):
каждая таблица в разрезе департамента несёт `department_id`, политики опираются
на транзакционную переменную `app.current_user_id`, RLS включён в режиме
`FORCE`, а приложение подключается ролью без прав суперпользователя и без
`BYPASSRLS`.

```rust
let user_id = session.require()?.id;               // из сессии ядра, не из аргументов команды
let mut tx = pool.begin().await?;
set_current_user(&mut tx, user_id).await?;        // set_config(…, true) — живёт до конца транзакции
let chunks = retrieval::retrieve(&mut tx, &embedding, &args.query, top_k).await?;
let lora   = adapters_for_user(&mut tx, llm, user_id).await?;
tx.commit().await?;
```

Три свойства, ради которых написано именно так:

- **закрыто по умолчанию**: переменная сессии не выставлена — политика не нашла
  ни одной строки, а не пропустила всё;
- **фильтры в SQL не являются границей безопасности**: явные условия по
  `department_id` в поисковых запросах нужны для скоупа и скорости ANN,
  единственная гарантия изоляции — политики RLS, и тест существует ровно для
  того, чтобы эту разницу удерживать;
- **переменная транзакционная**: при работе через пул `set_config(…, true)`
  откатывается в конце транзакции, поэтому личность одного запроса не может
  протечь в следующий, которому достанется то же соединение.

Тест `enclave/src-tauri/tests/rls_validation.rs` заводит двух пользователей в
разных департаментах и проверяет, что каждый видит только своё. Работает он в
одноразовой базе `enclave_rls_test` (drop + create в начале каждого прогона), а
не в рабочей: в прошлый раз его фикстуры навсегда осели в dev-базе.

```bash
cd enclave/src-tauri
export TEST_ADMIN_URL="postgres://postgres:postgres@localhost:5433/enclave"
export TEST_APP_URL="postgres://enclave_app:change_me_in_production@localhost:5433/enclave"
cargo test --test rls_validation
```

Без обеих переменных тест молча пропускается — если только не выставлена
`ENCLAVE_REQUIRE_DB_TESTS`: тогда отсутствие переменных — падение, а не
зелёный прогон, который ничего не доказывает. CI (`.github/workflows/ci.yml`)
выставляет её и гоняет тест на каждый push на PostgreSQL 16 + pgvector вместе с
unit-тестами и сборкой фронтенда.

Тот же результат руками, без сборки Rust, — видно, что политика закрыта по
умолчанию, а не фильтрует по запросу:

```
docs без app.current_user_id      0
docs под владельцем документов    5
docs под чужим user_id            0
```

## Поиск

Плотная ветка (косинусный ANN pgvector по `chunk_embeddings`) и лексическая
(полнотекстовый `tsvector` по `chunks.content_tsv`) выполняются по отдельности,
затем их ранги сливаются Reciprocal Rank Fusion в ядре на Rust: `1 / (k + rank)`,
k = 60. RRF работает по рангам, поэтому косинусное расстояние и полнотекстовый
ранг не нужно приводить к общей шкале — её у них нет (ADR-0006).

Каждая ветка берёт `2 × top_k` кандидатов, слияние отдаёт `top_k`. Ответ
возвращается вместе с `sources`, и интерфейс рисует их нумерованными цитатами:
пользователь видит, на чём основан ответ.

## Приём документов

Загрузка возвращает управление сразу и ставит задачу в `ingestion_jobs`
(ADR-0010). Конвейер:

```
upload ─► sha-256 ─► блоб {app_data}/blobs/{file_hash} ─► строка documents ─► задача
воркер ─► claim (FOR UPDATE SKIP LOCKED) ─► извлечение ─► нарезка ─► эмбеддинги
       ─► chunks + chunk_embeddings
```

Блоб пишется до вставки в базу, чтобы воркер никогда не увидел строку документа,
за которой ещё нет байтов на диске. Хранилище контентно-адресуемое: путь — это и
есть sha-256 содержимого, поэтому повторная загрузка того же файла не занимает
места второй раз.

Воркер — единственный компонент, который подключается ролью `ingest_worker`
(BYPASSRLS): он пишет фрагменты, когда никакой пользовательской сессии ещё нет,
и потому сам отвечает за проставление правильного `department_id` из
родительского документа (ADR-0009). При старте он возвращает в `queued` задачи,
застрявшие в `running` от упавшего прошлого процесса.

Ни одна транзакция базы не держится открытой на время HTTP-вызова к модели или
эмбеддеру — ни здесь, ни в поиске. Это инвариант, а не оптимизация: пул
соединений маленький, а вызов модели длинный.

**Форматы (ADR-0019, ADR-0020).** PDF с текстовым слоем, DOCX, таблицы
(XLSX, XLSM, XLSB, XLS, ODS), TXT и MD; формат определяется по содержимому
файла, а не по типу, который прислал браузер. Остальное (`.doc`, `.pptx`)
отклоняется сразу при загрузке, скан без
текстового слоя — ошибкой задачи с понятной причиной: OCR нет. Всё на чистом
Rust, без нативных библиотек в дистрибутиве. Штатный вывод PDF-парсера
разрывал слова («к алендарных»), поэтому текст из PDF собирается своим кодом —
по настоящим пробелам документа, а не по геометрии глифов. Проверить, что
извлечётся из конкретного файла, можно без загрузки:
`cargo run --example extract_text -- <файл>`. Счётчик токенов — по-прежнему
эвристика «символы / 4», помечена в коде.

**Таблицы (ADR-0020).** Каждая строка индексируется как самостоятельная
запись с листом, номером строки и заголовками столбцов:
`[лист «Зарплаты», строка 5] Имя: Иванов; Отдел: Продажи; Оклад: 120000` —
поэтому на вопрос о конкретной строке («сумма договора Д-012345») находится
именно она. Реестр из 50 000 строк индексируется за 9,5 минуты (эмбеддинги
пакетами, пакетная запись), поиск по нему — 0,2 секунды, для чего лексическая
ветка учитывает редкость слов (IDF), а политики RLS вычисляют членство один раз
на запрос (ADR-0021). Предел — 100 000 фрагментов на документ; ~12 КБ в базе
на строку.

**Расчёты по таблицам (ADR-0022).** «Сколько договоров заключено в 2021 году»,
«общая сумма договоров Петровой», «количество по статусам» не лежат ни в
одной строке, поэтому каждый лист хранится ещё и как таблица: столбцы с
выведенным типом (число, дата, текст), строки в JSONB, под теми же политиками
RLS. Если поиск нашёл строки таблицы, модель строит **план** — метрику
(count / sum / avg / min / max), группировку и фильтры. Её ответ ограничен
JSON-схемой, собранной из реальных столбцов этой таблицы, а в столбцах с
немногими значениями — ещё и из самих значений. Ядро проверяет план и
выполняет его как параметризованный агрегат на `app_pool`: **модель не пишет
SQL**, чужие строки отсекает RLS. Под ответом показано, что именно
посчитано. Нет плана — обычный RAG, а не приблизительное число. На Qwen 2.5 3B
все девять контрольных вопросов дали верные числа (сверено с SQL). После
перехода на Qwen3-4B (ADR-0024) модель трижды молча потеряла часть вопроса:
статус, фамилию, верхнюю границу года. Ядро теперь ловит и это: назван
столбец или его значение, а план их не использует; год назван периодом, а
условие по дате открыто. Во всех этих случаях задаётся вопрос, и все числа
снова сходятся с SQL.

Модель может неверно понять вопрос, поэтому ядро проверяет план (ADR-0023). Если
разбивка или считаемый столбец в вопросе не названы («кол-во договоров и
фамилия» — фамилии в «Ответственном» или в «Контрагенте»?), расчёт не
запускается: приходит вопрос с кнопками-столбцами. Каждая кнопка несёт готовый
план, и ядро проверяет его так же, как вывод модели. Ответ с разбивкой на группы
пишет ядро, а не модель: модель 3B склеивала список и теряла числа.

Чтобы вопрос сразу звучал словами таблицы, слева от чата есть панель документов.
У таблиц она раскрывается до листов и столбцов с типами, а клик по столбцу
вставляет «Ответственный» в вопрос. Из ядра приходят только названия и типы
(`cmd_list_document_tables`, под RLS), значений ячеек в панели нет. Клик по
самому документу ограничивает вопрос выбранными файлами: над полем ввода
появляются их чипы, поиск и расчёты смотрят только в них. RLS при этом решает
по-прежнему: чужой id не находит ничего. Векторная ветка для выбранных файлов
идёт через итеративное сканирование HNSW (pgvector 0.8). Обычное сканирование
сначала берёт ближайших соседей по всему индексу и только потом фильтрует, и
для одного файла среди 50 000 фрагментов может не вернуть ничего. Что найдёт
поиск, что посчитает и что ответит модель, можно проверить без интерфейса:
`cargo run --example retrieve -- <пользователь> "<вопрос>" --answer`.

## Инференс и LoRA

Поднимаются два процесса `llama-server`, а не один (ADR-0003): на 8080 —
резидентная чат-модель с предзагруженными LoRA-адаптерами
(`--lora-init-without-apply`), на 8081 — отдельная маленькая модель эмбеддингов
с `--embedding`. Разделять пришлось из-за размерности: `chunk_embeddings.embedding`
это `vector(768)` (ADR-0007), а скрытая размерность чат-модели совсем другая, и
переиспользование её под `/embeddings` дало бы векторы неверного размера.

Эмбеддер при старте регистрирует себя строкой в `embedding_models` и гасит
остальные активные: активная модель всегда ровно одна, потому что именно её
выбирает воркер загрузки.

Адаптеры выбираются на каждый запрос по департаментам пользователя, а в payload
уходит **целочисленный id, выданный самим сервером**, — он читается из
`GET /lora-adapters` и кэшируется как path → id. Раньше id выдумывался через
`ROW_NUMBER()` по строкам `department_adapters`, что ломается, как только один
файл адаптера назначен нескольким департаментам. Если сервер нужный путь не
загрузил, адаптер пропускается с предупреждением, а не подставляется наугад.

## Статус

| Этап | Содержание | Статус |
| --- | --- | --- |
| 1 | Каркас Tauri 2 + React + Rust, три пула, автоприменение миграций | готово |
| 2 | Схема: pgvector, HNSW + GIN, RLS-политики, роли (миграции 001-008) | готово |
| 3 | Изоляция департаментов и тест `rls_validation` на одноразовой БД | готово |
| 4 | Вход по PIN (argon2), локальные профили, права администратора | готово |
| 5 | Приём документов: блоб-хранилище, задачи, воркер, опрос статуса | готово: PDF, DOCX, XLSX / XLS / ODS, TXT, MD (без OCR) |
| 6 | Гибридный поиск + RRF, цитаты в интерфейсе, стриминг ответа | работает на живой модели; smoke-сценарий с двумя департаментами пройден — по журналу аудита ни одного чужого фрагмента |
| 7 | Sidecar'ы llama-server: чат + эмбеддинги, LoRA по департаментам | чат и эмбеддинги работают (llama.cpp b11124, Qwen3-4B, nomic-embed-text); движок под видеокарту и модели скачиваются при первом запуске (ADR-0024, ADR-0025), проверены CUDA и Vulkan; LoRA не проверена — нет ни одного адаптера |
| 8 | Расчёты по таблицам: план модели → параметризованный агрегат под RLS | готово; 12 контрольных вопросов на Qwen3-4B — числа верные (три — после уточнения) |
| 9 | Поставка PostgreSQL + pgvector вместе с приложением | работает в dev (ADR-0014): своя база без Docker — первый запуск, восстановление после сбоя, штатная остановка, сгенерированные пароли; включение в установщик и проверка на чистой машине — впереди |

**Как это читать.** Приложение поднимает оба сервера модели, принимает
документы и отвечает по ним с цитатами. Замер на RTX 4050: приём — 30–60
фрагментов в секунду, ответ — 0.5–1.1 сек при 67–76 ток/с. Сквозной сценарий
с двумя пользователями в разных департаментах пройден в интерфейсе, и проверен
он по журналу аудита, а не по тексту ответов: модель 3B способна подписать
цитату чужим именем файла, взятым из вопроса, — верен список `sources`, который
строит код. Не проверены LoRA-адаптеры: их пока нет ни одного.

Забытый PIN не восстановить — хранится только хэш argon2, — но можно заменить:
`cargo run --example reset_pin -- <имя>` (из `enclave/src-tauri`, нужен
`ADMIN_DATABASE_URL`; сброс пишется в журнал аудита).

Два решения ещё не записаны в ADR, хотя в коде уже работают: граница доверия
воркера загрузки и контентно-адресуемое хранилище блобов. Поставка PostgreSQL
решена и встроена (ADR-0014): без переменных `*_DATABASE_URL` приложение само
запускает урезанный PostgreSQL 18 со своей сборкой pgvector. Её первый живой
запуск нашёл ошибку, которую dev-база скрывала месяцами: регистрация модели
эмбеддингов писала колонку `provider`, которую не создавала ни одна миграция.
Она была только в dev-базе, когда-то собранной вручную из `db/schema.sql`. На
любой новой установке приём документов не работал бы. Исправлено миграцией
016. Тест `schema_contract` теперь выполняет стартовые запросы приложения на
базе, собранной только из миграций.
